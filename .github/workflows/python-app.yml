
name: Add or Delete new User n

on:
  workflow_dispatch:
    inputs:
      platform:
        type: choice
        description: 'Choose Platform'
        options:
        - All
        - coralogix
        - github
        - jira
        - redash
        - jenkins
        - aws
        - appsignal
        required: true
      if: ${{github.event.inputs.platform}}=="github"
      group:
        type: choice
        description: 'choose group'
        options:
        - group1
        - group2
        - group3
        

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
  
    - name: Run python script
      run: |
        
        echo "hello Rahul"
